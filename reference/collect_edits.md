# Flatten stamp_edits bundles and inline updates into one named list

Flatten stamp_edits bundles and inline updates into one named list

## Usage

``` r
collect_edits(dots)
```

## Arguments

- dots:

  List captured from `...`.

## Value

A named list of updates.
