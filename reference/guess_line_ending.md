# Guess line endings

Guess line endings

## Usage

``` r
guess_line_ending(content)
```

## Arguments

- content:

  Raw vector. File content.

## Value

Character. Line ending: `"\n"` (LF), `"\r"` (CR), or `"\r\n"` (CRLF).
