% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stamp-update.R
\name{stamp_edits}
\alias{stamp_edits}
\title{Bundle header edits for reuse}
\usage{
stamp_edits(...)
}
\arguments{
\item{...}{Named header edits. Each value is a new value or a function of the
field's current value (see \code{\link[=year_extend]{year_extend()}} and \code{\link[=author_add]{author_add()}}).}
}
\value{
A \code{stamp_edits} object.
}
\description{
Collects a set of named header updates into a reusable object you can inspect
and apply to one or many files with \code{\link[=stamp_update]{stamp_update()}}. Each edit is a new value
or a function of the field's current value, exactly as in \code{\link[=stamp_update]{stamp_update()}}.
}
\examples{
edits <- stamp_edits(copyright = year_extend(), author = author_add("Sam"))
edits

file <- tempfile(fileext = ".R")
writeLines(c("# Copyright (c) 2020", "# Author: Jane", "", "x <- 1"), file)
stamp_update(file, edits)
}
\seealso{
\code{\link[=stamp_update]{stamp_update()}}, \code{\link[=year_extend]{year_extend()}}, \code{\link[=author_add]{author_add()}}
}
