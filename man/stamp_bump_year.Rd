% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stamp-update.R
\name{stamp_bump_year}
\alias{stamp_bump_year}
\title{Bump the copyright year in a file header}
\usage{
stamp_bump_year(file, action = "modify", recursive = FALSE, pattern = NULL)
}
\arguments{
\item{file}{Character. Path to a file or directory to update.}

\item{action}{Character. Action to perform: "modify", "dryrun", or "backup".}

\item{recursive}{Logical. When \code{file} is a directory, descend into
subdirectories.}

\item{pattern}{Character or NULL. When \code{file} is a directory, only update
files whose name matches this pattern.}
}
\value{
TRUE invisibly on success, or a \code{stamp_update_preview} object when
\code{action = "dryrun"}.
}
\description{
A convenience wrapper around \code{\link[=stamp_update]{stamp_update()}} that extends the header's
copyright field to the current year, keeping the owner and start year (see
\code{\link[=year_extend]{year_extend()}}).
}
\examples{
file <- tempfile(fileext = ".R")
writeLines(c("# Copyright (c) 2020", "# Author: Jane", "", "x <- 1"), file)

stamp_bump_year(file)
readLines(file)[1]
}
\seealso{
\code{\link[=stamp_update]{stamp_update()}}, \code{\link[=stamp_add_author]{stamp_add_author()}}
}
