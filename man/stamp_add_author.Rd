% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stamp-update.R
\name{stamp_add_author}
\alias{stamp_add_author}
\title{Add an author to a file header}
\usage{
stamp_add_author(
  file,
  author,
  action = "modify",
  recursive = FALSE,
  pattern = NULL
)
}
\arguments{
\item{file}{Character. Path to a file or directory to update.}

\item{author}{Character. Author to add.}

\item{action}{Character. Action to perform: "modify", "dryrun", or "backup".}

\item{recursive}{Logical. When \code{file} is a directory, descend into
subdirectories.}

\item{pattern}{Character or NULL. When \code{file} is a directory, only update
files whose name matches this pattern.}
}
\value{
TRUE invisibly on success, or a \code{stamp_update_preview} object when
\code{action = "dryrun"}.
}
\description{
A convenience wrapper around \code{\link[=stamp_update]{stamp_update()}} that appends an author to the
header's author field without duplicating anyone already listed (see
\code{\link[=author_add]{author_add()}}).
}
\examples{
file <- tempfile(fileext = ".R")
writeLines(c("# Copyright (c) 2020", "# Author: Jane Doe", "", "x <- 1"), file)

stamp_add_author(file, "Sam Smith")
readLines(file)[2]
}
\seealso{
\code{\link[=stamp_update]{stamp_update()}}, \code{\link[=stamp_bump_year]{stamp_bump_year()}}
}
